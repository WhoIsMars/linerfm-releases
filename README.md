# LinerFM — Releases

Public distribution channel for LinerFM: macOS installers (`.dmg`) and the auto-updater appcast (`latest.json`).

This repository contains only shipped binaries. The application source is private.